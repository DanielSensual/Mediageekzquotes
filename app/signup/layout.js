import '@/app/globals.css';
import '../product/product.css';
import './signup.css';

export const metadata = {
    title: 'Sign Up — Video Quoter',
    description: 'Create your Video Quoter account. Set up in 30 seconds. 7-day free trial, no card required.',
};

export default function SignupLayout({ children }) {
    return children;
}
