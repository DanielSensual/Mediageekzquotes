export const metadata = {
    title: 'Project Thread — MediaGeekz',
    description: 'Your private project collaboration space. Track deliverables, review notes, and stay connected with your production team.',
    robots: { index: false, follow: false },
};

export default function ThreadLayout({ children }) {
    return children;
}
