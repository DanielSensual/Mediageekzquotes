export const metadata = {
    title: 'Convention & Event Coverage — Visual Treatment | MediaGeekz',
    description: 'Cinematic event coverage proposal for Prestige Realty Group. Three-tier video production packages for conventions, summits, and corporate events.',
};

export default function ProposalLayout({ children }) {
    return children;
}
