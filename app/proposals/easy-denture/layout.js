export const metadata = {
    title: 'Easy Denture — Mobile Dentistry Video Production Quote | MediaGeekz',
    description: 'Full-day mobile dentistry video production — capturing product demonstrations, patient testimonials, and interview-style content across multiple locations.',
    robots: { index: false, follow: false },
};

export default function ProposalLayout({ children }) {
    return children;
}
