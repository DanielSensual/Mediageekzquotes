export const metadata = {
    title: 'Andy The Coach — Blue Collar Business Consultants | MediaGeekz',
    description: 'Production proposal for Andy The Coach — live stream, multi-cam content, and brand strategy by MediaGeekz.',
};

export default function AndyLayout({ children }) {
    return children;
}
