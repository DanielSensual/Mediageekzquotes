export const metadata = {
    title: 'Wedding Film Coverage — Visual Treatment | MediaGeekz',
    description: 'Cinematic wedding film coverage proposal. Three-tier packages starting at $2,000 for single-shooter coverage.',
};

export default function WeddingProposalLayout({ children }) {
    return children;
}
